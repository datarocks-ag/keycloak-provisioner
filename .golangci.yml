version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

  settings:
    errcheck:
      exclude-functions:
        - (*net/http.Response).Body.Close
        - (io.Closer).Close
        - (*encoding/json.Encoder).Encode
        - (*encoding/json.Decoder).Decode
        - os.Unsetenv

issues:
  exclude-dirs:
    - vendor
